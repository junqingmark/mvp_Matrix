CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(mvp_Matrix)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/inc)

SET(SRCS 
	src/utils.c
	src/main.cpp
	src/imageReader.cpp)

ADD_EXECUTABLE(${PROJECT_NAME} ${SRCS})
TARGET_LINK_LIBRARIES(${PROJECT_NAME} EGL GLESv2 X11 png12 jpeg)
